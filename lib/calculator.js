/**
 * @module Calculator
 * */
export class Calculator {
    static { this.Add = (a, b) => a + b; }
    static { this.Subtract = (a, b) => a - b; }
    static { this.Multiply = (a, b) => a * b; }
    static { this.Divide = (a, b) => a / b; }
}
